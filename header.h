#include <corecrt_wstdio.h>
#include <stdio.h>

void solve(FILE *infle);
int getSumOfDigits(int value);
int readIntegerFromFile(FILE *infile);
int isIntegerPrime(int val);
void printResults(int a,int b);
